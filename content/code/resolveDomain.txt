fn resolveSolutionDomainEquationSystem(domainSystem, firstExpression) {
  domain = new SolutionDomain(firstExpression[0].solutionDomain)
  operator = '';
  currentEquation =firstExpression[1]

  do {
    [ domain, operator ] = addSubdomain(currentEquation.solutionDomain, operator, domain)
    currentEquation = equationSystem.get(operator)
  } while (currentEquation)

  return domain
}